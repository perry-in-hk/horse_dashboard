services:
  postgres:
    image: postgres:16-alpine
    container_name: hkjc-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-hkjc}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: ${POSTGRES_DB:-hkjc_dashboard}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-hkjc} -d ${POSTGRES_DB:-hkjc_dashboard}",
        ]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: hkjc-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 10

  backend:
    build:
      context: .
      dockerfile: infra/docker/backend.Dockerfile
    container_name: hkjc-backend
    env_file:
      - .env
    environment:
      # Hostname `postgres` is the DB service on the Compose network (see .env.example).
      SCRAPER_ROOT: /app/services/scraper
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL in .env}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      ODDS_SYNC_ENABLED: ${ODDS_SYNC_ENABLED:-true}
      ODDS_SYNC_INTERVAL_MS: ${ODDS_SYNC_INTERVAL_MS:-10000}
      ODDS_SYNC_ODDS_TYPES: ${ODDS_SYNC_ODDS_TYPES:-WIN,PLA}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:4000/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10

  frontend:
    build:
      context: .
      dockerfile: infra/docker/frontend.Dockerfile
    container_name: hkjc-frontend
    env_file:
      - .env
    environment:
      # Same-origin /api via Caddy on :80 (overrides .env VITE_API_URL for this stack).
      VITE_API_URL: ""
    depends_on:
      backend:
        condition: service_healthy

  caddy:
    image: caddy:2-alpine
    container_name: hkjc-caddy
    ports:
      - "80:80"
      # Uncomment when you add a DNS name + TLS block in infra/docker/Caddyfile:
      # - "443:443"
    volumes:
      - ./infra/docker/Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_started

  scraper:
    build:
      context: .
      dockerfile: infra/docker/scraper.Dockerfile
    container_name: hkjc-scraper
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      backend:
        condition: service_healthy

  recommender:
    build:
      context: .
      dockerfile: infra/docker/recommender.Dockerfile
    container_name: hkjc-recommender
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      backend:
        condition: service_healthy

volumes:
  postgres_data:
